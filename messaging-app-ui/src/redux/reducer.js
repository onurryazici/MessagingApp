import { Actions } from './actions';
import styles from '../styles.module.css';

export function reducer (state,action){
    switch(action.type){
        case '':
            return;
        default:
            return state;
    }
}